export default class Fetcher {
  constructor(params) {
        
  }
}

export class Websocket {
  constructor(params) {
    
  }
}

const tests = async () => {
  const params = {
    'wsEndpoint': 'wss://ws-feed-public.sandbox.gdax.com'
  }

}

